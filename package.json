{
  "name": "vite-chrome-extension",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prebuild": "node src/scripts/update-version.js",
    "build": "vite build",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
